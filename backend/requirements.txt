# Sentineth AI - runtime dependencies
#
# This file must stay UTF-8 (ASCII-only is safest). Do not regenerate it
# with `pip freeze > requirements.txt` in PowerShell: that writes UTF-16,
# which pip cannot read. Use one of these instead:
#   pip freeze | Out-File -Encoding utf8 requirements.txt
#   pip freeze > requirements.txt   # from bash / cmd, not PowerShell

# --- Web framework ---
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.3
python-multipart==0.0.32
pydantic==2.13.4
pydantic_core==2.46.4

# --- Database ---
SQLAlchemy==2.0.52
alembic==1.19.1
psycopg==3.3.4
psycopg-binary==3.3.4

# --- Vector store ---
qdrant-client==1.19.0

# --- Embeddings (active provider: LocalEmbeddingProvider) ---
# all-MiniLM-L6-v2, 384 dimensions. See AGENTS.md section 13-14.
sentence-transformers==6.0.0
transformers==5.15.0
# CPU-only installs are much smaller via the PyTorch CPU index:
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
numpy==2.4.6

# --- LLM / OpenAI-compatible clients (OpenAI + OpenRouter) ---
openai==1.109.1

# --- Document extraction ---
pypdf==6.16.1

# --- Config ---
python-dotenv==1.2.3

# --- Transitive pins ---
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
click==8.4.2
colorama==0.4.6
greenlet==3.5.5
h11==0.16.0
httpx==0.28.1
idna==3.18
Mako==1.4.1
MarkupSafe==3.0.3
typing-inspection==0.4.4
typing_extensions==4.16.0
tzdata==2026.3

pydantic-settings==2.15.0
prometheus-client==0.26.0
argon2-cffi==25.1.0
sentry-sdk==2.69.1
